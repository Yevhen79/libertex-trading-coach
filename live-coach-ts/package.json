{
  "name": "libertex-trading-coach",
  "version": "2.2.0",
  "private": true,
  "description": "Guardian — behavioural trading coach injected into the Libertex web terminal. Modular, typed TypeScript source (data / algorithm / UI). esbuild bundles it into two self-executing console files: RU and EN.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "check": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.6.3"
  }
}
